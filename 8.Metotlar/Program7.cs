using System;

namespace Metotlar
{
    class Program7
    {
        static void Main(string[] args)
        {
            var a = Math.E;//ln deki e sayısı
            var b = Math.PI;//Pi sayısı
            var c = Math.Abs(-5);//Mutlak Alma
            var d = Math.Ceiling(0.5);//Yukarı Yuvarlama
            var e = Math.Floor(0.5);//Aşagı Yuvarlama
            var f = Math.Max(0, 1);//Gelen Enbüyük değer
            var g = Math.Min(0, 1);//Gelen Enkücük değer
            var h = Math.Pow(2, 2);//Üssü
            var j = Math.Sqrt(4);//Karekök
            var k = Math.Log(8, 2);//Logaritma
            var l = Math.Exp(2);//e^2
            var m = Math.Log10(100);//10 tabanında Logaritma

        }
    }
}
